[package]
name = "opus-sys"
version = "0.1.0"
edition = "2021"
description = "FFI bindings to libopus (decode only)"
links = "opus"
build = "build.rs"

[build-dependencies]
cc = "1"
bindgen = "0.70"

[features]
default = []
static = []
